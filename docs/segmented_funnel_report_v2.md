# Segmented Funnel Report v2

Report date: 2026-05-12

## Scope

This report summarizes the segmented funnel after seeding synthetic validation traffic and rematerializing the analytics views.

Sources used:

- `session_facts`
- `funnel_rollup`
- `path_performance`
- `lead_scoring`
- `lead_score_summary`

## Important caveat

This report is synthetic-validation only and reflects traffic generated by `scripts/seed_sample_traffic.py`.

That means:

- the funnel logic and reporting structure can now be evaluated more meaningfully
- relative performance patterns are useful for validation
- absolute conversion rates should not be treated as real business outcomes

## Current sample size

- Total tracked sessions: 32
- Sessions with CTA click: 29
- Sessions with newsletter signup: 8
- Sessions with consultation lead: 19
- Scored consultation leads: 19

## Segment summary

### Highest consultation-driving segments

1. `/articles/visual-credibility` with `linkedin / social`
   - 10 sessions
   - 10 CTA clicks
   - 10 consultation leads
   - current consultation lead rate: 100%

2. `/for-women-in-finance` with `google / cpc`
   - 7 sessions
   - 7 CTA clicks
   - 7 consultation leads
   - current consultation lead rate: 100%

3. `/consultation-request` with `google / cpc`
   - 1 session
   - 1 consultation lead

### Highest newsletter-driving segments

1. `/articles/women-in-finance` with `google / cpc`
   - 2 sessions
   - 2 newsletter signups
   - current newsletter signup rate: 100%

2. `/articles/promotion-signals` across multiple sources
   - 3 sessions total across tracked sources
   - 3 newsletter signups
   - article-to-newsletter behavior is working as expected

## Path analysis

Top performing consultation paths:

1. `/articles/visual-credibility > /for-client-facing-leaders > /consultation-request`
   - 10 sessions
   - 10 consultation leads

2. `/for-women-in-finance > /consultation-request`
   - 8 sessions
   - 8 consultation leads

Top performing newsletter paths:

1. `/articles/women-in-finance > /newsletter`
   - 4 sessions
   - 4 newsletter signups

2. `/articles/promotion-signals > /newsletter`
   - 3 sessions
   - 3 newsletter signups

## Lead quality summary

Lead scoring shows two strong high-intent clusters:

1. `women_in_finance` from `google`
   - 7 leads
   - average lead score: `90.86`

2. `client_facing_credibility` from `linkedin`
   - 10 leads
   - average lead score: `78.6`

Additional older seed/test rows remain present:

- `consultation / google`: 1 lead, average score `82`
- `(unknown) / google`: 1 lead, average score `82`

## Interpretation

The synthetic sample supports the current funnel design:

1. Audience-specific paths are producing stronger downstream intent than generic browsing.
2. The finance-oriented path appears best aligned with the highest-quality leads.
3. The client-facing article path appears effective at driving consultation behavior.
4. Article-led journeys are a strong fit for newsletter capture.

## What this changes

Compared with the v1 report, we can now say the following with more confidence about the structure of the funnel itself:

- segmented audience paths are analyzable
- path-level conversion reporting is working
- lead scoring differentiates between strong lead clusters
- the project is ready for either real traffic collection or more targeted scenario testing

## Recommended next actions

1. If staying local, seed additional balanced traffic into `/for-client-facing-leaders`, `/for-women-in-finance`, and article pages to test alternative conversion mixes.
2. Continue using this report only for validation scenarios, not live business decisions.
3. Use `docs/segmented_funnel_report_live_v1.md` as the live-only reporting baseline.
4. Build a compact dashboard or notebook that reads directly from `funnel_rollup`, `path_performance`, and `lead_score_summary`.
